//Assume we are given two arrays of doubles, one of length 3 (position), the 
//other of length 4 (quaternion)

#include <iostream>
#include <math.h>

using namespace std;

int main() {
  double L_0 = 12.0;
  double d = 4.0;
  double l = 4.5;
  
  
}
